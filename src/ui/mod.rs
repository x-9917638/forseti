use iced::Element;

mod components;
mod landing;
mod main;
mod onboarding;

struct App {}

enum Message {}

impl App {
    fn update(&mut self, message: Message) {
        todo!()
    }
    fn view(&self) -> Element<'_, Message> {
        todo!()
    }
}
