use iced::Element;

mod generator;

pub struct Screen {}

enum Message {}

impl Screen {
    fn update(&mut self, message: Message) {
        todo!()
    }
    fn view(&self) -> Element<'_, Message> {
        todo!()
    }
}
